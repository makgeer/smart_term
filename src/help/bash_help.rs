use std::collections::HashMap;

pub struct BashHelp;

impl BashHelp {
    /// Получить полную справку по Bash командам
    pub fn get_full_help() -> String {
        let mut help = String::new();
        
        help.push_str("🎯 ПОЛНАЯ СПРАВКА ПО BASH КОМАНДАМ\n");
        help.push_str("═══════════════════════════════════\n\n");
        
        help.push_str(&Self::get_file_operations());
        help.push_str(&Self::get_text_processing());
        help.push_str(&Self::get_system_operations());
        help.push_str(&Self::get_network_commands());
        help.push_str(&Self::get_process_management());
        help.push_str(&Self::get_user_management());
        help.push_str(&Self::get_builtin_commands());
        help.push_str(&Self::get_common_examples());
        
        help
    }
    
    /// Получить быструю справку по часто используемым командам
    pub fn get_quick_reference() -> String {
        let mut quick = String::new();
        
        quick.push_str("⚡ БЫСТРАЯ СПРАВКА - ЧАСТО ИСПОЛЬЗУЕМЫЕ КОМАНДЫ\n");
        quick.push_str("═══════════════════════════════════════════════\n\n");
        
        let common_commands = vec![
            ("ls", "показать файлы", "ls -la"),
            ("cd", "сменить директорию", "cd /path"),
            ("pwd", "текущая директория", "pwd"),
            ("cp", "копировать файл", "cp file1 file2"),
            ("mv", "переместить файл", "mv old new"),
            ("rm", "удалить файл", "rm file"),
            ("mkdir", "создать папку", "mkdir folder"),
            ("cat", "показать файл", "cat file.txt"),
            ("grep", "поиск текста", "grep 'text' file"),
            ("find", "найти файлы", "find . -name '*.txt'"),
            ("ps", "процессы", "ps aux"),
            ("kill", "убить процесс", "kill -9 PID"),
            ("chmod", "права доступа", "chmod 755 file"),
            ("tar", "архивировать", "tar -czf archive.tar.gz folder/"),
        ];
        
        for (cmd, desc, example) in common_commands {
            quick.push_str(&format!("{:10} - {}\n", cmd, desc));
            quick.push_str(&format!("{:15} Пример: {}\n\n", "", example));
        }
        
        quick
    }
    
    /// Найти справку по конкретной команде
    pub fn search_command(command: &str) -> Option<String> {
        let commands: HashMap<&str, &str> = HashMap::from([
            // Файловые операции
            ("ls", "Показать содержимое директории\nОпции:\n  -l детальный список\n  -a показать скрытые\n  -h человеко-читаемый размер\nПример: ls -la /home"),
            ("cd", "Сменить текущую директорию\nПримеры:\n  cd /absolute/path\n  cd relative/path\n  cd ~ (домашняя директория)\n  cd .. (на уровень выше)"),
            ("pwd", "Показать текущую рабочую директорию"),
            ("cp", "Копировать файлы и директории\nОпции:\n  -r рекурсивно (для папок)\n  -i подтверждение перезаписи\nПримеры:\n  cp file1 file2\n  cp -r dir1 dir2"),
            ("mv", "Переместить или переименовать файлы\nПримеры:\n  mv oldname newname\n  mv file /target/directory/"),
            ("rm", "Удалить файлы и директории\nОпции:\n  -r рекурсивно\n  -f принудительно\n  -i интерактивно\nПример: rm -rf folder/"),
            ("mkdir", "Создать директорию\nОпции:\n  -p создать родительские директории\nПример: mkdir -p project/src/main"),
            ("rmdir", "Удалить пустую директорию"),
            ("touch", "Создать пустой файл или обновить время доступа"),
            
            // Работа с текстом
            ("cat", "Показать содержимое файла\nПримеры:\n  cat file.txt\n  cat file1 file2 > combined"),
            ("more", "Показать файл постранично"),
            ("less", "Показать файл с возможностью прокрутки"),
            ("head", "Показать первые N строк\nОпции:\n  -n число строк\nПример: head -n 10 file.txt"),
            ("tail", "Показать последние N строк\nОпции:\n  -n число строк\n  -f следить за изменениями\nПример: tail -f logfile"),
            ("grep", "Поиск текста по шаблону\nОпции:\n  -r рекурсивный поиск\n  -i игнорировать регистр\n  -n показать номера строк\nПример: grep -rn 'error' /var/log/"),
            ("find", "Поиск файлов и директорий\nПримеры:\n  find . -name '*.txt'\n  find /home -type f -size +1M\n  find . -mtime -7 (изменены за 7 дней)"),
            ("wc", "Подсчет строк, слов, байт\nОпции:\n  -l только строки\n  -w только слова\n  -c только байты\nПример: wc -l file.txt"),
            
            // Системные команды
            ("ps", "Информация о процессах\nОпции:\n  aux все процессы\n  -ef все процессы\nПример: ps aux | grep python"),
            ("top", "Интерактивный монитор процессов"),
            ("htop", "Улучшенный монитор процессов (требует установки)"),
            ("kill", "Завершить процесс\nСигналы:\n  -9 KILL (принудительно)\n  -15 TERM (корректно)\nПример: kill -9 1234"),
            ("killall", "Завершить процессы по имени\nПример: killall firefox"),
            ("df", "Информация о дисковом пространстве\nОпции:\n  -h человеко-читаемый\nПример: df -h"),
            ("du", "Размер файлов/директорий\nОпции:\n  -sh суммарный размер\nПример: du -sh /home/user/"),
            ("free", "Информация о памяти\nОпции:\n  -h человеко-читаемый\nПример: free -h"),
            
            // Сетевые команды
            ("ping", "Проверка доступности хоста\nПример: ping google.com"),
            ("curl", "Передача данных по URL\nПримеры:\n  curl -O http://example.com/file\n  curl -X POST -d 'data' http://api.example.com"),
            ("wget", "Скачивание файлов\nПример: wget http://example.com/file.zip"),
            ("ssh", "Подключение к удаленному хосту\nПример: ssh user@hostname"),
            ("scp", "Копирование файлов через SSH\nПример: scp file.txt user@host:/path/"),
            ("netstat", "Сетевые соединения\nОпции:\n  -tunlp все соединения\nПример: netstat -tunlp"),
            
            // Права доступа
            ("chmod", "Изменение прав доступа\nПримеры:\n  chmod 755 script.sh\n  chmod +x executable\n  chmod u=rw,go=r file.txt"),
            ("chown", "Изменение владельца\nПример: chown user:group file.txt"),
            ("sudo", "Выполнить команду с правами root\nПример: sudo apt update"),
            
            // Архивация
            ("tar", "Архивация файлов\nОпции:\n  -c создать архив\n  -x распаковать\n  -z gzip сжатие\n  -f указать файл\nПримеры:\n  tar -czf archive.tar.gz folder/\n  tar -xzf archive.tar.gz"),
            ("gzip", "Сжатие файлов\nПример: gzip file.txt"),
            ("zip", "Создание zip архива\nПример: zip -r archive.zip folder/"),
            
            // Переменные окружения
            ("export", "Установка переменной окружения\nПример: export PATH=$PATH:/new/path"),
            ("echo", "Вывод текста\nПример: echo $HOME"),
            ("env", "Показать переменные окружения"),
            
            // Встроенные команды Bash
            ("alias", "Создание псевдонимов\nПримеры:\n  alias ll='ls -la'\n  alias rm='rm -i'"),
            ("unalias", "Удаление псевдонима\nПример: unalias ll"),
            ("history", "История команд\nПримеры:\n  history\n  history 10 (последние 10)\n  !123 (выполнить команду №123)"),
            ("source", "Выполнить команды из файла\nПример: source ~/.bashrc"),
        ]);
        
        commands.get(command).map(|desc| {
            format!("📖 {}\n{}\n", command, desc)
        })
    }
    
    fn get_file_operations() -> String {
        r#"📁 ФАЙЛОВЫЕ ОПЕРАЦИИ
────────────────────────
ls           - список файлов
cd           - сменить директорию  
pwd          - текущая директория
cp           - копировать файлы
mv           - переместить/переименовать
rm           - удалить файлы
mkdir        - создать директорию
rmdir        - удалить пустую директорию
touch        - создать файл
file         - определить тип файла
stat         - информация о файле

Примеры:
  ls -la                    # Детальный список всех файлов
  cp -r dir1 dir2          # Рекурсивное копирование
  rm -rf folder/           # Принудительное удаление
  find . -name "*.txt"     # Найти все txt файлы

"#.to_string()
    }
    
    fn get_text_processing() -> String {
        r#"📝 ОБРАБОТКА ТЕКСТА
────────────────────
cat          - объединить и показать файлы
more         - постраничный просмотр
less         - улучшенный постраничный просмотр
head         - первые N строк
tail         - последние N строк
grep         - поиск по шаблону
sed          - потоковый редактор
awk          - обработка текста
sort         - сортировка строк
uniq         - уникальные строки
cut          - вырезать части строк
tr           - замена/удаление символов
wc           - подсчет строк/слов/символов

Примеры:
  grep "error" logfile     # Поиск ошибок в логе
  tail -f access.log       # Следить за логом в реальном времени
  wc -l file.txt           # Подсчет строк в файле
  sort file.txt | uniq     # Сортировка и удаление дубликатов

"#.to_string()
    }
    
    fn get_system_operations() -> String {
        r#"⚙️ СИСТЕМНЫЕ ОПЕРАЦИИ
─────────────────────
ps           - процессы системы
top          - монитор процессов
htop         - улучшенный монитор
kill         - завершить процесс
df           - место на дисках
du           - размер файлов/папок
free         - использование памяти
uname        - информация о системе
uptime       - время работы системы
who          - кто в системе
w            - кто и что делает

Примеры:
  ps aux | grep python     # Найти Python процессы
  df -h                    # Место на дисках (человеко-читаемо)
  du -sh /home/*           # Размер домашних директорий
  kill -9 1234             # Принудительно завершить процесс

"#.to_string()
    }
    
    fn get_network_commands() -> String {
        r#"🌐 СЕТЕВЫЕ КОМАНДЫ
──────────────────
ping         - проверить доступность
curl         - HTTP-запросы
wget         - скачать файл
ssh          - удаленное подключение
scp          - копирование через SSH
rsync        - синхронизация файлов
netstat      - сетевые соединения
ss           - информация о сокетах
ip           - управление сетью
ifconfig     - конфигурация интерфейсов
traceroute   - трассировка маршрута
dig          - DNS запросы
nslookup     - DNS lookup

Примеры:
  ping google.com           # Проверить доступность
  curl -I http://site.com   # Получить заголовки
  scp file user@host:/path  # Копировать на удаленный хост
  netstat -tunlp            # Все сетевые соединения

"#.to_string()
    }
    
    fn get_process_management() -> String {
        r#"🔄 УПРАВЛЕНИЕ ПРОЦЕССАМИ
─────────────────────────
&            - запуск в фоне
bg           - продолжить в фоне
fg           - вернуть на передний план
jobs         - список фоновых задач
nohup        - запустить устойчивый к разрыву
kill         - сигналы процессам
pkill        - убить по имени
killall      - убить все процессы с именем
nice         - запустить с измененным приоритетом
renice       - изменить приоритет

Примеры:
  ./script.sh &            # Запуск в фоне
  jobs                     # Показать фоновые задачи
  fg %1                    # Вернуть задачу 1 на передний план
  killall firefox          # Завершить все процессы Firefox

"#.to_string()
    }
    
    fn get_user_management() -> String {
        r#"👥 УПРАВЛЕНИЕ ПОЛЬЗОВАТЕЛЯМИ
──────────────────────────
whoami       - текущий пользователь
id           - информация о пользователе
groups       - группы пользователя
passwd       - сменить пароль
useradd      - добавить пользователя
userdel      - удалить пользователя
usermod      - изменить пользователя
groupadd     - добавить группу
su           - сменить пользователя
sudo         - выполнить как root

Примеры:
  whoami                    # Текущий пользователь
  id                        # Подробная информация
  sudo -u user command      # Выполнить команду как user
  passwd                    # Сменить пароль

"#.to_string()
    }
    
    fn get_builtin_commands() -> String {
        r#"🔧 ВСТРОЕННЫЕ КОМАНДЫ BASH
─────────────────────────
alias        - создать псевдоним
unalias      - удалить псевдоним
export       - установить переменную
unset        - удалить переменную
readonly     - сделать переменную readonly
declare      - объявить переменную
echo         - вывод текста
printf       - форматированный вывод
read         - чтение ввода
test         - проверка условий
[ ]          - альтернатива test
source       - выполнить файл
.            - аналогично source
exit         - выйти из shell
history      - история команд

Примеры:
  alias ll='ls -la'        # Создать псевдоним
  export PATH=$PATH:/new   # Добавить в PATH
  source ~/.bashrc         # Обновить настройки
  history | grep ssh       # Найти SSH команды в истории

"#.to_string()
    }
    
    fn get_common_examples() -> String {
        r#"💡 ПОЛЕЗНЫЕ ПРИМЕРЫ И КОМБИНАЦИИ
────────────────────────────────
# Поиск файлов и обработка
find . -name "*.java" -exec grep -l "class" {} \;

# Мониторинг логов в реальном времени
tail -f /var/log/syslog | grep -i error

# Архивирование с датой
tar -czf backup-$(date +%Y%m%d).tar.gz /important/data

# Просмотр больших файлов
less +F large_log_file.log

# Сетевая диагностика
ping -c 5 google.com && echo "Сеть работает"

# Управление процессами
ps aux --sort=-%mem | head -10  # Топ 10 по памяти

# Работа с текстом
grep -r "TODO" src/ | wc -l     # Подсчет TODO в коде

# Резервное копирование
rsync -av --progress /source/ /backup/

"#.to_string()
    }
}
